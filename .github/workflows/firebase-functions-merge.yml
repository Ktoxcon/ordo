# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Cloud Functions on merge
on:
  push:
    branches:
      - main
    paths:
      - backend/**
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - run: cd backend && pnpm install && pnpm build
      - name: Firebase Cloud Functions Deploy
        uses: Ktoxcon/action-cloud-functions-deploy@0.1.0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ORDO_12A39 }}
          channelId: live
          projectId: ordo-12a39
